# Use alpine latest for base image
FROM alpine:latest

# Add label
LABEL purpose="Minimal base image with NodeJS 22 for React Vite frontend deployment"

# Install NodeJS and npm
RUN apk add --no-cache nodejs npm
