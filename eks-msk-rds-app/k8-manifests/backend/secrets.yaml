apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: default
type: Opaque
data:
  # Base64 encoded password from environment variable
  SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD_BASE64}